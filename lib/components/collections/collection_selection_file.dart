import 'package:flutter/material.dart';

class CollectionSelectionFile extends StatelessWidget {
  const CollectionSelectionFile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
